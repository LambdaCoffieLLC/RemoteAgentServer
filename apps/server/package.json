{
  "name": "@remote-agent/server",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src/**/*.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@remote-agent/auth": "workspace:*",
    "@remote-agent/ports": "workspace:*",
    "@remote-agent/protocol": "workspace:*",
    "@remote-agent/providers": "workspace:*",
    "@remote-agent/sessions": "workspace:*"
  }
}
